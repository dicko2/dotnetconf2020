
name: "Testing"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
    - main    
    

jobs:
    build:
        name: Testing
        runs-on: [ubuntu-latest]
        steps:
        - run: echo 'Working'
